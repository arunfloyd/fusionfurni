<%- include('../layout/header.ejs') %>
    
<style>
  
    .containers {
        text-align: center;
        max-width: 400px;
        padding: 20px;
        background-color: #fff;
        border-radius: 15px;
        box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        margin: 0 auto; /* Center the container horizontally */
            margin-top: 10vh; 
    }

    .h4 {
        text-decoration: underline;
        color: #333;
    }

    .span {
        color: #ff5722; /* Highlighted color for RS:1000/- */
        font-weight: bold;
    }

   .p {
        margin: 15px 0;
        color: #666;
        font-size: 16px;
    }

    .a {
        text-decoration: none;
    }

    .gradient-button {
        padding: 10px 15px;
        background: linear-gradient(to right, #ff7e5f, #feb47b);
        color: #fff;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        display: inline-block;
        margin-top: 15px;
    }
</style>

<!-- Start Contact Form -->
<div class="untree_co-section">
    <div class="container">
        <h3>Personal Information & Earn Some Rewards </h3>
        <div class="block">
        
            
            </a>            <div class="row justify-content-center">
                <div class="col-md-8 col-lg-8 pb-4">
                    <form action="/profile" method="post" id="mainForm">
                      <div class="row">
                        <div class="col-6">
                          <div class="form-group">
                            <label class="text-black" for="fname">Name</label>
                            <input type="text" class="form-control" id="fname"  value="<%=profile.name%>" readonly>
                          </div>
                        </div>
                        <div class="col-6">
                          <div class="form-group">
                              <label class="text-black" for="email">Email address</label>
                              <input type="email" class="form-control" id="email" value="<%=profile.email%>"readonly>
                          </div>
                        </div>
                        <div class="col-6">
                          <div class="form-group">
                            <label class="text-black" for="fname">Phone Number</label>
                            <input type="text" class="form-control" id="fname" value="<%=profile.mobile%>"readonly>
                          </div>
                      </div><br><br><br><br>
                        <label class="text-black" for="fname">Saved Address </label>
                        <div class="row">
                          <% addresses.forEach((address, index) => { %>
                              <div class="col-md-4 mb-4">
                                  <div class="card" style="width: 18rem; margin-bottom: 15px;">
                                      <!-- Add margin-bottom property above -->
                                      <div class="card-body">
                                          <h6 class="card-subtitle mb-2 text-body-secondary">
                                              <label class="text-black" for="fname"><%= address.name %></label>
                                          </h6>
                                          <p class="card-text">
                                              <%= address.mobile %><br>
                                              <%= address.street %><br>
                                              <%= address.area %><br>
                                              <%= address.landmark %><br>
                                              <%= address.pincode %><br>
                                              <%= address.addressType %>
                                            
                                              

                                          </p>
                                          
                                          <a href="#" class="card-link remove-address" data-address-id="<%= address._id %>">Remove</a> 
                                          <a href="#" class="card-link edit-address" data-address-id="<%= address._id %>">Edit</a> 

                                                                             </div>
                                  </div>
                              </div>
                              <% if ((index + 1) % 3 === 0) { %>
                                  </div><div class="row">
                              <% } %>
                          <% }); %>
                      </div>
                      
                    </div>
                        <div class="form-check mb-3">
                            <input class="form-check-input" type="checkbox" id="addAddressCheckbox">
                            <label class="form-check-label" for="addAddressCheckbox">
                                Add Address
                            </label>
                        </div>

                        <div id="addressFields" style="display: none;">
                            <div class="form-group mb-3">
                                <label class="text-black" for="name">Name</label>
                                <input type="text" name="name" class="form-control"  id="name">
                                <span id="nameError" class="text-danger"></span>
                            </div>
                        
                            <div class="form-group mb-3">
                                <label class="text-black" for="mobile">Phone Number</label>
                                <input type="text" name="mobile" class="form-control" id="mobile" >
                                <span id="mobileError" class="text-danger"></span>
                            </div>
                        
                            <div class="form-group mb-3">
                                <label class="text-black" for="street">Flat, House no., Building, Company, Apartment</label>
                                <input type="text" name="street" class="form-control" id="street" >
                                <span id="streetError" class="text-danger"></span>
                            </div>
                        
                            <div class="form-group mb-3">
                                <label class="text-black" for="area">Area, Street, Sector, Village</label>
                                <input type="text" name="area" class="form-control" id="area" >
                                <span id="areaError" class="text-danger"></span>
                            </div>
                        
                            <div class="form-group mb-3">
                                <label class="text-black" for="landmark">Landmark</label>
                                <input type="text" name="landmark" class="form-control" id="landmark" >
                                <span id="landmarkError" class="text-danger"></span>
                            </div>
                        
                            <div class="form-group mb-3">
                                <label class="text-black" for="pincode">Pincode</label>
                                <input type="text" name="pincode" class="form-control" id="pincode" >
                                <span id="pincodeError" class="text-danger"></span>
                            </div>
                        
                            <div class="form-group mb-3">
                                <label class="text-black" for="addressType">Address Type</label>
                                <select name="addressType" class="form-control" id="addressType">
                                    <option value="home" >Home - 9:00 AM to 5:00 PM Delivery</option>
                                    <option value="work" >Work - 9:00 AM to 9:00 PM Delivery</option>
                                </select>
                                <span id="addressTypeError" class="text-danger"></span>
                            </div>
                            <button type="submit" class="btn btn-primary">Submit</button>
                            <br>
                            <br>

                        </div>                     
<a href="/edit-profile"><button type="button" class="btn btn-primary-hover-outline">Edit Profile</button>
</a>
                </div>
            </div>
        </div>
        <div class="text-center">
        <div class="containers">
            <h4 class="h4">Refer And Earn upto <span class="span">RS:1000/-</span></h4>
            <p class="p">Share this link with your friend, and after their successful signup, you will get Rs: 100 in your wallet.</p>
            <p class="p">So Let's Go...</p>
            <a href="#" class="a" onclick="copyLink()"><button class="btn btn-warning">Share The Link</button> </a>        </div>
    </div>  
    </div>
</div>
</div>
<%- include('../layout/footer.ejs') %>

<!-- End Contact Form -->
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.3/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

<script>
    function copyLink() {
        var linkText = "http://localhost:3000/register/<%= profile._id %>";
        var tempInput = document.createElement("input");
        tempInput.value = linkText;
        document.body.appendChild(tempInput);
        tempInput.select();
        document.execCommand("copy");
        document.body.removeChild(tempInput);

        alert("Link copied to clipboard!");
    }
</script>
<script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
<script>
     function editAddress(address) {
        document.getElementById('name').value = address.name;
        document.getElementById('mobile').value = address.mobile;
        // Populate other form fields similarly

        openModal();
    }
    $(document).ready(function () {
        $("#addAddressCheckbox").change(function () {
            if (this.checked) {
                $("#addressFields").show();
            } else {
                $("#addressFields").hide();
            }
        });

        $(".remove-address").on("click", function (event) {
            event.preventDefault();
            const confirmed = confirm("Are you sure you want to remove this address?");
            if (confirmed) {
                const addressId = $(this).data("address-id");
                window.location.href = "/remove-address/" + addressId;
            }
        });
        $(".edit-address").on("click", function (event) {
            event.preventDefault();
            const confirmed = confirm("Are you sure you want to remove this address?");
            if (confirmed) {
                const addressId = $(this).data("address-id");
                window.location.href = "/edit-address/" + addressId;
            }
        });
      
    });
</script>
<script>
    document.addEventListener('DOMContentLoaded', function () {
        const form = document.querySelector('form');
        form.addEventListener('submit', function (event) {
            // Validate Name
            const nameInput = document.getElementById('name');
            if (!validateName(nameInput.value)) {
                event.preventDefault();
                displayError(nameInput, 'Name should be valid');
            } else {
                clearError(nameInput);
            }

            const mobileInput = document.getElementById('mobile');
            if (!validateMobile(mobileInput.value)) {
                event.preventDefault();
                displayError(mobileInput, 'Phone number should be 10 digits');
            } else {
                clearError(mobileInput);
            }
            const pincodeInput = document.getElementById('pincode');
            if (!validatePincode(pincodeInput.value)) {
                event.preventDefault();
                displayError(pincodeInput, 'Pincode should be 6 digits');
            } else {
                clearError(pincodeInput);
            }
            const streetInput = document.getElementById('street');
            const areaInput = document.getElementById('area');
            const landmarkInput = document.getElementById('landmark');

            if (!validateLength(streetInput.value, 10)) {
                event.preventDefault();
                displayError(streetInput, 'Should be more than 10 characters');
            } else {
                clearError(streetInput);
            }

            if (!validateLength(areaInput.value, 10)) {
                event.preventDefault();
                displayError(areaInput, 'Should be more than 10 characters');
            } else {
                clearError(areaInput);
            }

            if (!validateLength(landmarkInput.value, 10)) {
                event.preventDefault();
                displayError(landmarkInput, 'Should be more than 10 characters');
            } else {
                clearError(landmarkInput);
            }

        });
        function validateLength(value, minLength) {
            return value.trim().length > minLength;
        }
        function validateName(name) {
            return name.trim() !== '' && name.length >= 3;
        }

        function validateMobile(mobile) {
            return /^\d{10}$/.test(mobile);
        }

        function displayError(input, message) {
            const errorElement = document.getElementById(input.id + 'Error');
            errorElement.textContent = message;
        }

        function clearError(input) {
            const errorElement = document.getElementById(input.id + 'Error');
            errorElement.textContent = '';
        }
        function validatePincode(pincode) {
            // Add your custom validation logic here
            return /^\d{6}$/.test(pincode);
        }
    });
</script>